# Codex Home Assistant Add-on

A Home Assistant add-on package that runs OpenAI Codex CLI in a browser terminal via `ttyd`, with persistent sessions via `dtach`.

Inside the terminal or over SSH, use `ai` to open or reattach the persistent Codex session.
